# AGENTS.md

> 本文件供 AI 代理在本仓库工作时参考。
> 可加入 `.gitignore` 作为本地私有约定，也可提交到仓库作为团队共享规范。

## 一、构建与测试

> 以下为示例命令，请替换为当前项目实际的工具链（Maven / Gradle / npm / go / cargo 等）。

```bash
# 编译（跳过测试）
<build_cmd> compile

# 日常开发：编译 + 单元测试
<build_cmd> test

# 打包
<build_cmd> package

# 启动应用
<run_cmd>
```

**每次交付前需满足**：

- 编译通过
- 现有测试全绿
- 本次变更点有对应的测试或验证证据

## 二、通用编码规范

### 2.1 日志：统一使用日志门面

- 类上加 `@Slf4j`（Lombok）或等价注解，直接用 `log.info / warn / error / debug`。
- 不手动声明 `private static final Logger log = LoggerFactory.getLogger(...)`。
- 日志要可定位：包含关键标识（主键、业务 ID、状态变更前后值），能从日志回溯到具体操作与源码位置。
- 日志内容尽量用中文

### 2.2 导入与类型声明

- 类型一律在文件顶部 `import` 后使用短类名。
- 不在方法签名、返回值、变量声明、泛型实参里写完整包路径（如 `java.util.List<com.xxx.Dto>`）。
- 仅当两个同名不同包类型必须共存、且无法通过重命名规避时，才对其中一个使用全限定名。

### 2.3 工具方法归属

- 可复用的无状态工具（字符串 / 集合 / 时间 / JSON / 格式化 / 编号转换等）统一放到 `xxx.utils` 包。
- 工具类声明为 `final` + 私有构造，方法用 `public static`；不注入依赖、不持有可变状态。
- 业务实现类（Service / Controller / Helper 等）不沉淀通用工具方法；修改时发现同类存量方法应一并迁移。
- 依赖数据库、事务、外部服务、Spring Bean 的逻辑属于业务，不要为形式统一强行迁入工具类。

### 2.4 异常与事务

- 核心业务流程出错抛异常并中断；非核心调用失败用 `try/catch` + `log.warn` 降级，不影响主流程。
- 跨多表的写操作用 `@Transactional(rollbackFor = Exception.class)`；只读或单表简单写入无需加。
- 异步任务（通知、回调、定时）注意幂等与重试，事务只加在真正拥有多表写入边界的方法上。
- 抛出的异常信息和日志记录尽量用中文

### 2.5 时间与格式

- 日期时间格式通过常量统一定义，不在代码中散写字面量。建议约定：

| 用途     | 模式                      |
|----------|---------------------------|
| 日期     | `yyyy-MM-dd`              |
| 日期时间 | `yyyy-MM-dd HH:mm:ss`     |
| 带毫秒   | `yyyy-MM-dd HH:mm:ss.SSS` |

## 三、代码组织原则

> 具体包名按项目实际调整，以下为分层职责约定（适用于多数后端项目）。

```
入口层（Controller / CLI / Job / 路由）  ← 协议适配、参数校验、路由分发
    ↓
业务层（Service / 用例编排）              ← 用例步骤、事务边界、外部调用、异常降级
    ↓
数据层（Repository / Mapper / DAO）       ← 数据访问语义、查询条件封装、批量处理
    ↓
实体 / 数据库
```

- **入口层**只做协议转换和入口校验，不拼 SQL、不编排骨骼事务、不直接操作数据层底层。
- **业务层**负责一个完整用例的步骤顺序、事务边界、状态变化和外部调用；读取数据通过数据层接口，不在业务类里直接构造查询条件。
- **数据层**是数据访问的唯一归属；复杂查询、分页、批量、保留字处理在这里完成，不把查询构造暴露给业务层。
- 按聚合或稳定的数据职责划分数据访问接口，不创建只转发 `selectById` 的万能 Base 类，也不为单个私有方法新增一层抽象。
- 类过大时按职责拆分 Helper / 子服务，避免单类膨胀；简单的两三行转换和单次调用保留原处，避免方法碎片化。
- 严格关注代码格式化，包括 方法多参数换行对齐、JavaDoc 注释等格式化。

## 四、注释与文档

- 注释、JavaDoc 尽量使用中文（或与项目既有风格保持一致）。
- 新增类的类级注释应包含：中文类说明、`@author Macro Ray`、`@since <yyyy-MM-dd>`。
- 公开方法提供清晰的 JavaDoc，写清参数含义、返回值语义。
- 复杂业务逻辑补行内注释，说明"为什么这么做"，而不是复述"做了什么"。

## 五、通用底线约定

> 以下为保证项目长期可维护性的最低要求，改动时请遵守。

1. **不破坏既有公开契约**：对外 API、接口的字段名 / 语义 / 返回结构改动前必须确认；如确需变更，记录差异、原因与影响范围。
2. **避免魔法值**：状态、类型等用枚举或常量表示，不在业务代码中直接写字面量（SQL 语句内无法引用常量时可豁免）。
3. **大数 ID 用长整型**：可能超过 21 亿的 ID 用 `Long` / `long`，不用 `int` / `Integer`；仅确知是小数字（如序号 1~200）时可用 `Integer`。
4. **关键写操作留日志**：数据库写入和重要状态变更前后留 INFO 日志，至少包含：实体类型、主键、变更前后值、关联业务 ID。
5. **改完即验证**：每次改动后跑一次编译 + 相关测试，确认未引入回归后再继续下一步。
6. **小步前进**：单个批次改动控制在合理范围（建议 < 15 文件），大规模变更拆分为多个可独立验证的小任务。

## 六、文档管理

- 项目文档按职责分类存放（如 `docs/architecture`、`docs/business`、`docs/engineering`），不在根目录散落业务文档。
- 每次完成多批次或较大变更后，在对应目录补一份记录文档：任务范围、变更点、验证结果、遗留风险、后续工作。
- 本文件（AGENTS.md）只维护长期有效的规则与约定， **不写**临时计划、执行进度、完成百分比或待办项——这些放到 docs 或任务系统。

## 七、工作流程

1. 改动前先读懂现有实现与依赖，确认改动范围和影响面。
2. 按分层约定放置代码：入口层不写业务逻辑，业务层不拼 SQL。
3. 编译 + 测试验证（见第一节命令）。
4. 更新相关文档与注释，不留过时描述。
5. 重大或跨模块变更留一份记录文档。

---
